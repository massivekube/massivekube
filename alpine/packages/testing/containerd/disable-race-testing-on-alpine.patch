diff --git a/Makefile.linux b/Makefile.linux
index 23dffad..cf3cd51 100644
--- a/Makefile.linux
+++ b/Makefile.linux
@@ -22,6 +22,6 @@ ifeq ($(GOOS),linux)
 endif

 # amd64 supports go test -race
-ifeq ($(GOARCH),amd64)
-	TESTFLAGS_RACE= -race
-endif
+#ifeq ($(GOARCH),amd64)
+#	TESTFLAGS_RACE= -race
+#endif
